// TODO: Starter for Level 05: POST with JSON payloads.

// Placeholder: students should set Content-Type and send JSON in Postman.
console.log(
  "Using the POST method with JSON payloads requires setting the content type to application/json and sending the data in JSON format to get the proper responses.",
);
